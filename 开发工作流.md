# 休息提醒 — 开发工作流

> 创建时间：2026-06-20

## 目录结构

```
Desktop\休息提醒\    ← 唯一版本（开发+运行都在这里）
```

## 核心规则

1. **直接运行工作版** — 修改和运行都在同一个目录，开发调试 → `python rest_reminder.py`，日常使用 → `dist\RestReminder.exe`
2. **修改后先 Python 运行验证** — `python rest_reminder.py` 直接跑，看界面是否正常
3. **确认没问题再打包** — 打包只是为了让用户不用装 Python，不是必须的

## 快速启动方式

| 方式 | 命令 | 适用场景 |
|------|------|---------|
| Python 直接运行 | `cd C:\Users\binlo\Desktop\休息提醒 && python rest_reminder.py` | 开发调试 |
| Python 静默模式 | `python rest_reminder.py --silent` | 测试托盘+浮球 |
| exe 运行 | `dist\RestReminder.exe` | 用户日常使用 |

## 打包

```bash
pyinstaller RestReminder.spec
# 输出在 dist/RestReminder.exe
```

## 数据文件同步

开发版和 exe 版共用同一目录下的 `.json` 文件，无需手动同步。
