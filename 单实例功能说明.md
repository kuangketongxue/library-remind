# 休息提醒程序 - 单实例功能说明

## 功能概述

程序现在具有**单实例运行保护**，确保同一时间只有一个程序实例在运行，避免以下问题：
- ✓ 防止多个提醒窗口同时弹出
- ✓ 防止系统托盘出现多个图标
- ✓ 减少资源占用
- ✓ 提供更好的用户体验

## 工作原理

### 1. 锁文件机制
程序启动时会在临时目录创建锁文件：
- **位置**：`%TEMP%\rest_reminder.lock`
- **内容**：当前运行程序的进程ID（PID）

### 2. 启动检查流程
```
程序启动
    ↓
检查锁文件是否存在
    ↓
存在 → 读取PID → 验证进程是否运行 → 是 → 显示提示并退出
    ↓                                    ↓
不存在                                  否
    ↓                                    ↓
创建锁文件 ← ← ← ← ← ← ← ← ← ← ← ← ← ← ←
    ↓
正常启动程序
```

### 3. 智能清理
- 程序正常退出时自动删除锁文件
- 如果检测到锁文件中的PID已不存在，自动清理旧锁文件
- 如果锁文件损坏，自动删除并重新创建

## 使用场景

### 场景1：正常启动
```
用户操作：双击启动程序
结果：程序正常启动并显示窗口
```

### 场景2：重复启动（非静默模式）
```
用户操作：程序已运行，再次双击启动
结果：显示对话框提示"程序已在运行中"，并提供解决方案
```

### 场景3：重复启动（静默模式）
```
系统操作：开机自启动时，程序已在运行
结果：静默退出，不显示任何提示（避免干扰用户）
```

### 场景4：异常退出后重启
```
情况：程序异常退出，锁文件残留
结果：自动检测PID无效，清理旧锁文件，正常启动
```

## 提示信息

### 命令行提示
当检测到程序已运行时，会在命令行显示：
```
休息提醒程序已经在运行中！
如果您看到此消息但找不到程序窗口，请：
1. 检查系统托盘（任务栏右下角）是否有程序图标
2. 双击托盘图标可显示窗口
3. 或者先结束已运行的进程再重新启动
```

### 对话框提示（非静默模式）
```
标题：程序已在运行
内容：
休息提醒程序已经在运行中！

请检查系统托盘（任务栏右下角）是否有程序图标。
双击托盘图标可显示窗口。

如需重新启动，请先右键托盘图标选择"退出"。
```

## 工具脚本

### 1. 清理多余进程.bat
**用途**：清理所有运行中的Python后台进程和锁文件

**使用场景**：
- 程序出现多个实例
- 程序无法正常启动
- 需要强制重启程序

**操作步骤**：
1. 双击运行 `清理多余进程.bat`
2. 查看当前运行的进程列表
3. 按任意键确认清理
4. 等待清理完成
5. 重新启动程序

### 2. 测试单实例.bat
**用途**：测试单实例功能是否正常工作

**测试流程**：
1. 第一次启动程序（后台静默模式）
2. 等待3秒
3. 第二次启动程序（前台模式）
4. 检查是否显示"程序已在运行中"提示
5. 自动清理测试进程

**预期结果**：
- 第一次启动成功
- 第二次启动被阻止，显示提示信息
- 系统托盘只有一个程序图标

## 常见问题

### Q1：如何确认程序是否在运行？
**A**：检查系统托盘（任务栏右下角）是否有程序图标。

### Q2：程序提示已运行，但找不到窗口？
**A**：
1. 检查系统托盘，双击图标显示窗口
2. 如果托盘也没有图标，运行 `清理多余进程.bat` 清理后重启

### Q3：如何重新启动程序？
**A**：
1. 右键系统托盘图标，选择"退出"
2. 重新启动程序

### Q4：开机自启动会不会重复启动？
**A**：不会。如果程序已在运行，开机自启动会静默退出，不会显示任何提示。

### Q5：锁文件会不会一直占用空间？
**A**：不会。锁文件很小（只有几个字节），且程序退出时会自动删除。

### Q6：如果程序崩溃，锁文件会残留吗？
**A**：可能会残留，但下次启动时会自动检测并清理无效的锁文件。

## 技术细节

### 依赖库
- `psutil`：用于进程管理和验证
- `os`：文件操作
- `tempfile`：获取临时目录路径
- `atexit`：注册退出清理函数

### 锁文件格式
```
<PID>
```
例如：`12345`

### 进程验证逻辑
```python
1. 检查PID是否存在
2. 获取进程对象
3. 检查进程命令行是否包含 'rest_reminder'
4. 如果都满足，则确认程序已在运行
```

### 清理时机
- 程序正常退出时（通过 `atexit` 注册）
- 检测到无效锁文件时
- 手动运行清理脚本时

## 兼容性

- ✓ Windows 10/11
- ✓ Python 3.6+
- ✓ PyQt5
- ✓ 支持静默启动模式
- ✓ 支持开机自启动

## 更新日志

### 2026-04-30
- ✓ 实现单实例运行机制
- ✓ 添加文件锁和PID验证
- ✓ 创建清理和测试工具
- ✓ 完善提示信息

---

**如有任何问题，请运行 `清理多余进程.bat` 或查看 `修复说明.md`。**
