# 电脑提醒应用开发最佳实践

## 概述
这是从完整的休息提醒应用开发过程中总结出来的经验教训，确保创建的应用不会出现以下问题：
- ❌ 挂件太小/太大，内容显示不完整
- ❌ 重启/开机就消失，自启动不工作
- ❌ 关掉IDE就消失，无法持续运行
- ❌ 窗口被最小化后看不到内容
- ❌ 托盘图标消失
- ❌ 用户可以轻易退出应用

---

## 一、基础架构设计

### 1.1 核心功能要求
每个电脑提醒应用都应该包含：
- ✅ **定时提醒**：按指定间隔（如每小时）触发提醒
- ✅ **特定时间提醒**：如23点睡觉提醒
- ✅ **状态监控**：如充电状态、音频设备健康
- ✅ **弹窗提示**：友好的用户提醒界面
- ✅ **持久运行**：应用持续后台运行

### 1.2 技术栈推荐
- **GUI框架**：Python Tkinter（简单、跨平台）
- **系统集成**：Windows API（ctypes调用）
- **后台运行**：pythonw.exe（无控制台窗口）
- **版本控制**：Git（每个稳定版本打标签）

---

## 二、窗口设计最佳实践

### 2.1 窗口尺寸
**问题**：窗口太小内容被截断，太大占用空间

**解决方案**：
```python
# 推荐窗口尺寸（根据内容调整）
self.expanded_size = (420, 450)  # 宽度×高度，单位像素

# 设置窗口几何尺寸
self.root.geometry(f"{self.expanded_size[0]}x{self.expanded_size[1]}")
```

### 2.2 文字折行
**问题**：文字太长超出窗口边界

**解决方案**：
```python
# 设置合适的折行长度，比窗口宽度小一点
wraplength = 390  # 对应420宽度的窗口

# Label示例
tk.Label(container, textvariable=self.detail_var, 
         font=("Microsoft YaHei UI", 11), 
         bg="#F6F7FB", fg="#2F3A4F", 
         justify="left", wraplength=390).pack(...)
```

### 2.3 防止窗口隐藏/最小化
**问题**：用户点击关闭或隐藏后，窗口消失看不到

**解决方案**：
```python
# 1. 修改 _hide_to_tray - 禁用隐藏功能
def _hide_to_tray(self) -> None:
    pass  # 什么都不做

# 2. 修改 _collapse_widget - 禁用最小化功能
def _collapse_widget(self) -> None:
    self.is_collapsed = False  # 始终不处于折叠状态
    # 不执行任何最小化操作

# 3. 修改 _on_window_unmapped - 不要把状态设为collapsed
def _on_window_unmapped(self, event: tk.Event | None = None) -> None:
    if not self.root.winfo_exists():
        return
    self.is_collapsed = False  # 保持显示状态
```

### 2.4 窗口位置和可见性保障
**问题**：窗口跑到屏幕外或被最小化

**解决方案**：
```python
def _ensure_window_visible(self) -> None:
    # 强制窗口处于正常显示状态
    self.is_collapsed = False
    if not self.root.winfo_exists():
        return
    try:
        self.root.deiconify()  # 取消最小化
        self.root.state("normal")  # 强制正常状态
        self.root.lift()  # 提升到顶层
        self.root.focus_force()
        
        # 强制设置尺寸和位置
        width = self.expanded_size[0]
        height = self.expanded_size[1]
        self._dock_to_right(width, height)
        self.root.geometry(f"{width}x{height}")
        self.root.update_idletasks()
        
        # 使用Windows API确保窗口可见
        hwnd = self.root.winfo_id()
        user32 = ctypes.windll.user32
        SW_RESTORE = 9
        SWP_SHOWWINDOW = 0x0040
        left, top, right, bottom = get_work_area()
        x = max(left, right - width - 8)
        y = max(top, top + ((bottom - top - height) // 2))
        user32.ShowWindow(hwnd, SW_RESTORE)
        user32.SetWindowPos(hwnd, 0, x, y, width, height, SWP_SHOWWINDOW)
        user32.SetForegroundWindow(hwnd)
    except Exception:
        pass
```

### 2.5 定时检查窗口状态
**问题**：窗口可能意外消失

**解决方案**：
```python
def _visibility_guard_tick(self) -> None:
    if not self.root.winfo_exists():
        return
    # 强制窗口始终显示
    self.is_collapsed = False
    try:
        needs_restore = self.root.state() != "normal"
    except tk.TclError:
        needs_restore = True
    if not needs_restore:
        try:
            self.root.update_idletasks()
            x, y = self.root.winfo_x(), self.root.winfo_y()
            width = self.root.winfo_width()
            height = self.root.winfo_height()
            left, top, right, bottom = get_work_area()
            # 检查窗口是否在屏幕外或尺寸不对
            if (x < left - 4 or y < top - 4 or 
                x + width > right + 4 or y + height > bottom + 4 or
                width < 400 or height < 400):
                needs_restore = True
        except tk.TclError:
            needs_restore = True
    if needs_restore:
        self._ensure_window_visible()
    # 每5秒检查一次
    self.root.after(5000, self._visibility_guard_tick)
```

---

## 三、系统托盘图标管理

### 3.1 托盘图标创建
**问题**：托盘图标消失，无法恢复窗口

**解决方案**：
```python
class TrayIconController:
    def __init__(self, tooltip: str, on_restore_request, on_exit_request):
        self.tooltip = tooltip
        self.on_restore_request = on_restore_request
        self.on_exit_request = on_exit_request
        self._ready = threading.Event()
        self._stop = threading.Event()
        self._restore_requested = threading.Event()
        self._exit_requested = threading.Event()
        self._thread = threading.Thread(target=self._run, 
                                       name="TrayIconController", 
                                       daemon=True)
        self._wndproc = None
        self._class_name = f"BreakReminderTray_{os.getpid()}"
        self._hwnd = None
        self._nid = None
        self._icon_handle = None
        self._add_ok = False
        self._icon_path = _create_tray_icon_file()  # 创建图标文件
        self._thread.start()
        self._ready.wait(timeout=5)
    
    def _add_icon(self) -> None:
        # 添加托盘图标
        shell32 = ctypes.windll.shell32
        self._nid = self._build_notify_data()
        self._add_ok = bool(shell32.Shell_NotifyIconW(NIM_ADD, 
                                                       ctypes.byref(self._nid)))
        self._nid.uTimeoutOrVersion = NOTIFYICON_VERSION_4
        shell32.Shell_NotifyIconW(NIM_SETVERSION, ctypes.byref(self._nid))
```

### 3.2 托盘图标自动恢复机制
**问题**：托盘图标可能意外消失

**解决方案**：
```python
def _check_and_restore_icon(self) -> bool:
    """检查托盘图标状态，如消失则重新添加"""
    try:
        shell32 = ctypes.windll.shell32
        if not self._hwnd:
            return False
        if not self._add_ok:
            self._add_icon()
            return self._add_ok
        # 尝试修改来检查图标是否存在
        test_tip = self.tooltip + " "
        self._nid.szTip = test_tip
        result = bool(shell32.Shell_NotifyIconW(NIM_MODIFY, 
                                                 ctypes.byref(self._nid)))
        if not result:
            # 图标消失，重新添加
            self._remove_icon()
            self._nid.szTip = self.tooltip
            self._add_icon()
            return self._add_ok
        # 恢复原来的tooltip
        self._nid.szTip = self.tooltip
        shell32.Shell_NotifyIconW(NIM_MODIFY, ctypes.byref(self._nid))
        return True
    except Exception:
        return False

# 在主循环中每30秒检查一次
def _tick(self) -> None:
    # ... 其他代码
    if (now.second % 30) == 0:  # 每30秒
        self._tray_icon.check_and_restore()
```

---

## 四、开机自启动实现

### 4.1 三重保障机制
**问题**：自启动经常失效

**解决方案**：同时实现三种方式
```python
def install_autostart() -> str:
    script = Path(__file__).resolve()
    python_exe = Path(sys.executable)
    # 使用pythonw.exe（无控制台窗口后台运行）
    pythonw = python_exe.with_name("pythonw.exe")
    launcher = pythonw if pythonw.exists() else python_exe
    command = f'"{launcher}" "{script}"'
    startup_dir = Path.home() / "AppData" / "Roaming" / "Microsoft" / \
                  "Windows" / "Start Menu" / "Programs" / "Startup"
    
    results = []
    
    # 方式1：注册表启动
    key_path = r"Software\Microsoft\Windows\CurrentVersion\Run"
    try:
        with winreg.CreateKeyEx(winreg.HKEY_CURRENT_USER, key_path, 0, 
                               winreg.KEY_SET_VALUE) as key:
            winreg.SetValueEx(key, "BreakReminderApp", 0, winreg.REG_SZ, 
                             command)
            saved_value, _ = winreg.QueryValueEx(key, "BreakReminderApp")
        if saved_value == command:
            results.append("registry")
    except OSError:
        pass
    
    # 方式2：VBS启动文件（在Startup文件夹）
    try:
        startup_dir.mkdir(parents=True, exist_ok=True)
        vbs_file = startup_dir / "BreakReminderApp.vbs"
        vbs_content = 'Set WshShell = CreateObject("WScript.Shell")\r\n' \
                     f'WshShell.Run """" & "{launcher}" & """ """ & ' \
                     f'"{script}" & """", 0, False\r\n'
        vbs_file.write_text(vbs_content, encoding="utf-16")
        results.append("startup-vbs")
    except OSError:
        pass
    
    # 方式3：BAT启动文件（备用）
    try:
        bat_file = startup_dir / "BreakReminderApp.bat"
        bat_content = f'@echo off\r\nstart "" {command}\r\n'
        bat_file.write_text(bat_content, encoding="gbk")
        results.append("startup-bat")
    except OSError:
        pass
    
    if results:
        return "+".join(results)
    return "failed"
```

### 4.2 程序启动时确保自启动已安装
```python
def ensure_autostart() -> None:
    try:
        install_autostart()
    except OSError:
        pass

# 在main()函数开头调用
def main() -> None:
    # ... 其他代码
    ensure_autostart()  # 确保自启动已安装
    # ... 启动应用
```

---

## 五、单实例运行控制

### 5.1 使用Windows互斥体
**问题**：多个实例同时运行，造成混乱

**解决方案**：
```python
MUTEX_NAME = "Global\\BreakReminderWidgetSingleton"

def ensure_single_instance() -> bool:
    """确保只有一个实例在运行，返回True表示是第一个实例"""
    global _singleton_mutex_handle
    handle = ctypes.windll.kernel32.CreateMutexW(None, False, MUTEX_NAME)
    if not handle:
        return True
    _singleton_mutex_handle = handle
    return ctypes.windll.kernel32.GetLastError() != ERROR_ALREADY_EXISTS

def activate_existing_window() -> bool:
    """尝试激活已存在的窗口"""
    user32 = ctypes.windll.user32
    hwnd = user32.FindWindowW(None, REMINDER_WINDOW_TITLE)
    if not hwnd:
        return False
    _restore_window(hwnd)
    return True

def _close_app(self) -> None:
    # ... 其他清理代码
    # 释放互斥体
    global _singleton_mutex_handle
    if _singleton_mutex_handle:
        try:
            ctypes.windll.kernel32.CloseHandle(_singleton_mutex_handle)
        except Exception:
            pass
```

### 5.2 在main()中使用
```python
def main() -> None:
    args = parse_args()
    _set_app_user_model_id()
    
    # 检查单实例
    if not ensure_single_instance():
        activate_existing_window()
        return
    
    # 确保自启动
    ensure_autostart()
    
    # ... 启动应用
```

---

## 六、持久运行机制

### 6.1 禁用用户退出
**问题**：用户可以轻易退出应用

**解决方案**：
1. 删除GUI中的"退出"按钮
2. 删除托盘菜单中的"退出"选项
3. 禁用窗口关闭按钮的退出功能

```python
# 窗口关闭只隐藏（但我们已经禁用隐藏了）
self.root.protocol("WM_DELETE_WINDOW", self._hide_to_tray)

# 托盘菜单只保留"恢复"选项
def _show_context_menu(self, hwnd):
    # ... 创建菜单
    # 只添加"恢复"选项，不添加"退出"
    mii_restore = MENUITEMINFO()
    mii_restore.cbSize = ctypes.sizeof(MENUITEMINFO)
    mii_restore.fMask = MIIM_STRING | MIIM_ID
    mii_restore.wID = 1001
    mii_restore.dwTypeData = "恢复"
    mii_restore.cch = len(mii_restore.dwTypeData)
    user32.InsertMenuItemW(hmenu, 0, True, ctypes.byref(mii_restore))
```

### 6.2 使用后台模式运行
**问题**：关闭控制台窗口就停止程序

**解决方案**：
```bash
# 使用pythonw.exe而不是python.exe
pythonw break_reminder.py

# 或者用VBS脚本调用，完全隐藏窗口
Set WshShell = CreateObject("WScript.Shell")
WshShell.Run "pythonw.exe C:\path\to\break_reminder.py", 0, False
```

---

## 七、完整版本管理策略

### 7.1 使用Git标签管理稳定版本
```bash
# 每次修改后提交并打标签
git add .
git commit -m "v1.0-stable: 初始版本"
git tag -a v1.0-stable -m "v1.0-stable"

# 查看所有版本
git tag -l

# 切换到特定版本
git checkout v1.0-stable

# 查看最近提交
git log --oneline -10
```

### 7.2 维护CHANGELOG.md
```markdown
# 休息提醒应用 更新日志

## v1.6-stable (2026-04-21)
### 修复
- 修复窗口被最小化后看不到内容的问题
- 禁用隐藏到托盘功能，始终保持窗口可见
- 禁用最小化功能，防止窗口消失

## v1.5-stable (2026-04-21)
### 改进
- 删除"退出"按钮，强制应用持续运行
- 删除托盘图标右键菜单中的"退出"选项

## v1.4-stable (2026-04-21)
### 修复
- 修复乱码/问号问题，恢复所有中文显示
- 增加窗口尺寸，确保所有内容完整显示

## v1.3-stable (2026-04-21)
### 改进
- 添加托盘图标自动恢复机制
- 改进开机自启动功能（三重保障）

## v1.2-stable (2026-04-21)
### 新增
- 完整系统托盘图标功能

## v1.1-stable (2026-04-21)
### 修复
- 修复窗口大小问题
- 添加互斥体释放和临时文件清理

## v1.0-stable (2026-04-21)
### 初始版本
- 核心功能：每小时提醒、23点睡觉提醒、充电检测
- 系统托盘、音频监控、弹窗提醒、开机自启动、单实例
```

---

## 八、测试清单

发布前必须测试：
- [ ] 窗口尺寸合适，内容完整显示
- [ ] 文字折行正确，不超出边界
- [ ] 点击关闭按钮不会隐藏窗口
- [ ] 点击"隐藏"按钮不会最小化
- [ ] 窗口始终保持可见状态
- [ ] 托盘图标正常显示
- [ ] 托盘图标每30秒能自动恢复（如消失）
- [ ] 开机自启动正常工作
- [ ] 重启电脑后应用自动启动
- [ ] 多个实例不会同时运行
- [ ] 用户无法轻易退出应用
- [ ] 关闭控制台窗口程序继续运行
- [ ] 所有中文正常显示，无乱码/问号
- [ ] 核心功能（定时提醒、状态检测）正常工作

---

## 九、常见问题及解决方案

| 问题 | 解决方案 |
|------|----------|
| 窗口太小/太大 | 设置合适的尺寸（如420×450），文字折行适配 |
| 重启后消失 | 实现三重自启动（注册表+VBS+BAT） |
| 关闭IDE就消失 | 使用pythonw.exe后台运行 |
| 窗口被最小化看不到 | 禁用最小化/隐藏功能，强制is_collapsed=False |
| 托盘图标消失 | 实现check_and_restore机制，每30秒检查 |
| 用户可以退出 | 删除退出按钮和菜单选项 |
| 中文乱码/问号 | 确保源文件和资源使用正确编码 |
| 多个实例同时运行 | 使用Windows互斥体实现单实例控制 |

---

## 十、快速开始模板

### 创建新提醒应用的步骤：
1. 复制break_reminder.py作为模板
2. 修改功能逻辑（提醒时间、检测内容等）
3. 调整窗口尺寸（确保所有内容可见）
4. 保持所有持久化机制不变（自启动、单实例、托盘等）
5. 测试所有检查清单项
6. 提交Git并打标签

### 关键文件：
- `break_reminder.py` - 主程序
- `CHANGELOG.md` - 版本历史
- `电脑提醒应用开发最佳实践.md` - 本文档（经验总结）

---

## 十一、重要代码速查

### 获取工作区
```python
def get_work_area() -> tuple[int, int, int, int]:
    rect = RECT()
    ok = ctypes.windll.user32.SystemParametersInfoW(SPI_GETWORKAREA, 0, 
                                                     ctypes.byref(rect), 0)
    if ok:
        return int(rect.left), int(rect.top), int(rect.right), int(rect.bottom)
    screen_w = int(ctypes.windll.user32.GetSystemMetrics(0))
    screen_h = int(ctypes.windll.user32.GetSystemMetrics(1))
    return 0, 0, screen_w, screen_h
```

### 停靠窗口到右侧
```python
def _dock_to_right(self, width: int, height: int) -> None:
    left, top, right, bottom = get_work_area()
    x = max(left, right - width - 8)
    y = max(top, top + ((bottom - top - height) // 2))
    self.root.geometry(f"{width}x{height}+{x}+{y}")
```

---

**总结**：只要严格按照这些最佳实践开发，就不会再出现"挂件太小/太大""重启/开机就消失""关掉就消失"等问题！
