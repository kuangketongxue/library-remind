# 🚀 后台运行说明

## ✅ 已实现功能

程序现在可以**完全独立后台运行**，关闭终端也不会影响程序！

## 📋 启动方式

### 方式一：一键后台启动（推荐）
双击运行：`一键后台启动.bat`

**特点：**
- ✅ 完全静默启动
- ✅ 不显示任何窗口
- ✅ 关闭终端不影响运行
- ✅ 只在系统托盘显示图标

### 方式二：后台启动（带提示）
双击运行：`后台启动.bat`

**特点：**
- ✅ 显示启动提示信息
- ✅ 3秒后自动关闭
- ✅ 关闭终端不影响运行

### 方式三：直接运行 VBS
双击运行：`完全独立启动.vbs`

**特点：**
- ✅ 最简洁的启动方式
- ✅ 完全静默
- ✅ 双击即启动

## 🔧 技术原理

### 为什么关闭终端也能继续运行？

1. **使用 pythonw 而不是 python**
   - `pythonw.exe` 是无窗口版本的 Python
   - 不依赖控制台窗口
   - 完全后台运行

2. **使用 VBS 脚本启动**
   - VBS 使用 `WScript.Shell` 启动程序
   - 参数 `0` 表示隐藏窗口
   - 参数 `False` 表示不等待程序结束
   - 启动后立即返回，程序独立运行

3. **使用 start 命令（BAT 方式）**
   - `start "" /B` 在后台启动进程
   - 进程不依赖父进程
   - 关闭 CMD 窗口不影响子进程

## 🎯 开机自启动

运行 PowerShell 脚本：`setup_autostart_now.ps1`

**自启动特点：**
- ✅ 开机自动启动
- ✅ 完全静默，不显示窗口
- ✅ 只在系统托盘显示图标
- ✅ 使用 VBS 脚本，确保独立运行

## 📱 如何使用

### 启动后：
1. 程序在系统托盘（任务栏右下角）显示图标
2. 双击托盘图标 → 显示/隐藏主窗口
3. 右键托盘图标 → 显示菜单（显示/隐藏、重置位置、退出）

### 主窗口功能：
- 显示距离下次休息的倒计时
- 显示学习时长统计
- 显示电池状态和电量
- 可以拖动窗口位置
- 点击 "−" 按钮最小化到任务栏

## ⚙️ 进程管理

### 查看程序是否在运行
打开任务管理器，查找 `pythonw.exe` 进程

### 停止程序
- 方式一：右键托盘图标 → 退出
- 方式二：任务管理器中结束 `pythonw.exe` 进程

### 单实例保护
- 程序自动检测是否已经运行
- 如果已运行，再次启动会提示
- 防止多个实例同时运行

## 🔍 验证独立运行

### 测试步骤：
1. 运行 `一键后台启动.bat`
2. 关闭所有 CMD 窗口
3. 检查系统托盘是否有程序图标
4. 双击托盘图标，查看主窗口是否正常显示
5. 等待一段时间，确认倒计时正常运行

### 预期结果：
✅ 关闭终端后，程序继续运行
✅ 托盘图标正常显示
✅ 主窗口可以正常显示/隐藏
✅ 倒计时正常工作
✅ 到时间会自动打开视频

## 💡 常见问题

### Q: 关闭终端后程序还在运行吗？
A: 是的！使用 `pythonw` + VBS 启动，程序完全独立运行。

### Q: 如何确认程序在运行？
A: 查看系统托盘（任务栏右下角）是否有程序图标。

### Q: 如何显示主窗口？
A: 双击系统托盘的程序图标。

### Q: 如何完全退出程序？
A: 右键托盘图标，选择"退出"。

### Q: 开机自启动后找不到窗口？
A: 自启动是静默模式，只显示托盘图标。双击托盘图标即可显示窗口。

## 🎉 总结

现在你可以：
- ✅ 双击 `一键后台启动.bat` 启动程序
- ✅ 关闭终端，程序继续运行
- ✅ 设置开机自启动，完全自动化
- ✅ 随时通过托盘图标控制程序

**享受你的休息提醒吧！** 🎊
