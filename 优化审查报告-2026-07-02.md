# 休息提醒 — 全面优化审查报告

> 审查时间：2026-07-02
> 范围：rest_reminder.py、feishu_calendar.py、storage.py、tray_card.py
> 方法：第一性原理 + 对抗性审查（正确性 / 完整性 / 健壮性）

---

## 一、本次已修复/优化项

### P0：飞书日程状态混淆（用户截图问题）

**现象**：今日 tab 中同时出现 "⚠️ 日程获取失败" 与 "今日暂无日程安排"，用户无法判断是无法获取还是真的没有日程。

**根因**：
1. `FeishuCalendarManager.get_display_text()` 仅检查 `self._error_msg`（且成功获取后会被清空），没有独立的失败状态位。
2. 失败时 `_events` 保留旧值；若旧值为空，则 `get_display_text` 返回失败，但 `_update_calendar_list` 可能因判断顺序或状态刷新时序走空日程分支。
3. 没有区分 "从未同步 / 同步中 / 同步成功但为空 / 同步失败" 四种状态。

**修复**：
- 新增 `self._fetch_failed` 状态位与 `fetch_failed` 属性。
- 新增 `get_status()` 返回结构化状态：`disabled / syncing / ok / empty / error`。
- 失败状态优先于空状态：`get_display_text()` 在 `_fetch_failed=True` 时返回 `⚠️ 无法获取日程：{具体原因}`。
- 手动刷新时立即重置失败标记，UI 先进入 "正在同步..."，避免持续显示旧错误。
- `_update_calendar_list()` 按 `kind` 分支显示：失败原因 / 同步中 / 空日程 / 日程列表，并为状态标签设置对应颜色（红/蓝/灰/绿）。

### P1：主界面视觉层次与一致性

| 优化项 | 修改前 | 修改后 |
|--------|--------|--------|
| 数据卡片 `_make_stat_card` | 纵向 emoji + 数值 + 标题 | 横向圆形色块图标 + 数值/标题 |
| 状态卡片 | 统一浅色文字，emoji 前缀 | 状态色区分（学习绿/休息橙/暂停金/待机灰），加粗 |
| 22:00 倒计时 | emoji 标题，小字时间 | 纯文字标题，剩余时间放大加粗 |
| 复盘摘要 | emoji 图标 | 文字标签 + 颜色区分最佳/最低 |
| 连续打卡 | emoji 火焰图标 | 文字 "连" 图标 |
| 主内容区/顶部栏/tab 占位区/滚动区 | 硬编码 `#0d0d12` | 使用 `THEMES[self._current_theme]['bg_base']` |

### P2：代码清理

- 移除 `rest_reminder.py` 中重复的 `import tempfile`。

---

## 二、仍存在的可优化点（未在本次改动）

### P0：单实例锁双轨并存

- **位置**：`SingleInstanceChecker`（约 1460 行）
- **问题**：AGENTS.md 写明使用 `CreateMutexW` 内核级互斥锁，但代码中仍保留 `msvcrt.locking` 文件锁作为 fallback。文件锁在 Windows 上可能因路径/权限竞争失败，且 `msvcrt` 已明确不再使用。
- **建议**：删除文件锁分支，仅保留 Named Mutex；移除 `import msvcrt` 及相关 `_lock_handle`/`_lock_path` 逻辑。

### P0：硬编码 dark 主题颜色大面积残留

- **位置**：`_md_to_html` 代码块（2680-2710 行）、复盘/成就/设置对话框、AI 报告 `QTextBrowser` 等。
- **问题**：在 light 主题下，这些区域仍显示深底色 + 浅色文字，导致可读性差。
- **建议**：将 `_md_to_html` 改为接受 `theme` 参数，或读取全局当前主题，使用 `THEMES` 变量生成 HTML 内联样式；其他硬编码 `#0d0d12` / `#18181f` / `#e8e4dc` 全部替换为主题 token。

### P1：`rest_reminder.py` 单文件过大（≈7600 行）

- **问题**：所有 UI、逻辑、状态机、网络、AI、报表、图表都在一个文件，编译/维护/并行开发成本高。
- **建议**：分模块拆分（风险高，需分阶段）：
  - `ui/tabs.py`：各 tab 构建
  - `core/timer.py`：状态机与计时
  - `core/ai.py`：AI 报告
  - `ui/widgets.py`：通用组件（卡片、开关、图表）
  - 先抽取 `_make_stat_card`、`_make_toggle`、`_make_section_header` 等纯 UI 工厂到独立文件。

### P1：异常处理过于宽泛

- **位置**：大量 `except Exception:` / `except (RuntimeError, Exception): pass`
- **问题**：吞掉本可诊断的错误，尤其是 `_refresh_general_tab`、`_update_calendar_list` 等每秒调用的函数，静默失败时日志也没有。
- **建议**：高频刷新函数保留容错，但应记录 `log.warning`；非高频/关键路径改为捕获具体异常。

### P1：AI 报告与网络请求无统一超时/重试

- **位置**：`_call_ai`、各 provider 调用
- **问题**：用户已配置多个 provider，但失败时只展示最后一个错误；无指数退避；无本地缓存过期策略。
- **建议**：增加 provider 状态缓存（5 分钟失败冷却），避免反复调用已知的不可用服务；缓存 AI 报告结果按日期+数据 hash 失效。

### P1：设置项保存触发过于频繁

- **位置**：每个 `_toggle_*` 都直接 `LocalSync.save_settings`
- **问题**：快速切换多个开关时产生多次 JSON 写入。
- **建议**：引入 500ms 防抖，合并多次连续写入。

### P2： TrayCard 仍重度依赖 emoji

- **位置**：`tray_card.py`（菜单图标、提示）
- **问题**：项目已在主界面去 emoji，但托盘卡片仍使用大量 emoji，跨系统字体不一致。
- **建议**：与主界面统一，emoji 图标替换为文字图标或 QPainter 绘制的矢量图标。

### P2：成就/趋势 tab 延迟加载后未预加载数据

- **位置**：`_switch_tab` 延迟构建
- **问题**：首次切换到"趋势"tab 时仍有明显空白等待，热力图/柱状图数据未在后台预取。
- **建议**：启动 3 秒后在后台线程预计算趋势数据，切到 tab 时直接渲染。

### P2：日志与诊断

- **问题**：`crash.log` 只在 `excepthook` 写入，但大量异常被 `log.error` 后未进入 `crash.log`，排查启动失败时仍需看 `rest_reminder.log`。
- **建议**：统一错误入口，所有 `log.error` 同时写入 `crash.log` 滚动缓存。

---

## 三、主界面信息架构建议（第一性原理）

当前 "今日" tab 的元素顺序：

1. 标题 + 副标题
2. 学习时长 / 当前轮次 / 休息时长（3 卡片）
3. 状态卡片
4. 飞书日程卡片
5. 距离 22:00 倒计时
6. 复盘摘要（条件）
7. 连续打卡

**建议调整**：
- 将 "连续打卡" 从底部移到数据卡片行，与 "学习时长" 并列，形成更均衡的 4 卡片首屏。
- 状态卡片与计时器可合并到数据卡片下方，减少卡片数量。
- 飞书日程卡片在启用时才有价值，未启用时应折叠或显示轻量提示，避免占用首屏空间。
- 22:00 倒计时作为次要信息，可缩小为细条放在状态卡片下方。

---

## 四、验证结果

- `py_compile` 语法检查：`rest_reminder.py` ✅、`feishu_calendar.py` ✅
- 单实例测试：后台启动成功，PID 16168 运行正常，无 `crash.log` ✅
- 飞书日程状态测试：syncing / empty / error / disabled 四种状态区分通过 ✅

---

## 五、后续建议优先级

1. **立即处理**：统一硬编码主题为 `THEMES` 变量（影响 light 主题可用性）。
2. **下一版**：清理单实例锁遗留文件锁代码。
3. **版本迭代中**：拆分 `rest_reminder.py`，先从通用组件工厂开始。
