# 版本记录 - 休息提醒

**版本: v2.8**
**日期**: 2026-05-28

## 改进

### 1. 移除飞书同步，改为全本地持久化
- **背景**：飞书同步依赖 lark-cli（Node.js 子进程调用），在 pythonw 上下文中频繁出现 .cmd 包装器失败、环境变量丢失、GBK 编码崩溃等问题，多次修复仍有隐患
- **方案**：移除整个 `FeishuSync` 类（~250 行）+ `lark-cli` 依赖 + 飞书凭据配置，替换为 `LocalSync` 类（~50 行），学习时长和电脑使用时长存入 `.daily_log.json`
- **收益**：
  - 学习时长跨重启恢复（之前重启归零）
  - 无网络依赖，无 lark-cli 报错
  - 代码量减少 ~200 行
  - 无需配置飞书环境变量/config.json
- **删除文件**：`config.json`（飞书凭据）、`.feishu_cache.json`
- **保留文件**：`.computer_usage.json`（电脑使用缓存不变）、`.daily_log.json`（新增）

---

**版本: v2.7**
**日期**: 2026-05-28

## 修复

### 1. lark-cli .cmd 包装器在 pythonw 中失败（主因，100% 飞书同步失败）
- **问题**：pythonw 子进程中 `shell=True` 无法正确解析 `.cmd` 文件，stderr 输出 GBK 乱码 `'包装�?REM' 不是内部或外部命令`，返回码 255
- **修复**：`_call_lark` 新增 Node.js 直接调用 fallback（`node scripts/run.js`），绕过 .cmd 包装器。先尝试 shell=True，失败后自动切换到 Node.js

### 2. 环境变量丢失导致飞书功能禁用
- **问题**：pythonw 子进程可能继承不到 `setx` 设的 `FEISHU_BASE_TOKEN` / `FEISHU_TABLE_ID`，日志显示 `未设置飞书同步凭据`
- **修复**：新增 `config.json` 兜底机制——环境变量优先，缺失时从项目目录的 `config.json` 读取

### 3. @file 路径校验失败（彻底消除）
- **问题**：lark-cli 1.0.39+ 偶发将 `@.feishu_tmp.json` 相对路径转绝对路径后拒绝
- **修复**：始终使用内联 JSON 传递数据，不再写临时文件，彻底消除路径校验问题

---

**版本: v2.6**
**日期**: 2026-05-27

## 修复

### 1. SingleInstanceChecker 作用域导致程序反复关闭（主因）
- **问题**：`SingleInstanceChecker` 是 `main()` 局部变量，函数继续执行后被 GC 回收 → `cleanup()` 删除锁文件 → watchdog 误判主进程已死 → 反复重启 → 最终 watchdog 也退出 → 程序彻底停止
- **修复**：移到模块级变量 `_single_instance`，生命周期与进程一致

### 2. watchdog 干净退出后放弃守护
- **问题**：`proc.returncode == 0 and runtime > 10` 时直接 break，干净退出被视为"用户主动关闭"
- **修复**：改为继续守护，只有用户通过托盘菜单退出才会终止看门狗

### 3. 视频获取 100% 失败（re 变量未绑定）
- **问题**：`get_bilibili_videos()` 在 QThread 中执行时，模块级 `re` 偶发 UnboundLocalError（疑似 Python 3.14 线程安全问题）
- **修复**：函数内 `import re as _re` 本地导入兜底

### 4. lark-cli @file 路径校验失败
- **问题**：lark-cli 1.0.39 偶发将相对路径转绝对路径后拒绝
- **修复**：`@file` 失败时自动回退为内联 JSON；lark-cli 已更新到 1.0.41

### 5. WindowsApps 代理导致看门狗双实例
- **问题**：`pythonw.exe` 是 Microsoft Store 代理（< 100KB），实际调用真实 Python → 看门狗自己启动自己 → 两个 watchdog + 两个 rest_reminder
- **修复**：watchdog.py 和 rest_reminder.py 中检测 WindowsApps 代理文件大小，自动从 PATH 中找真实 Python

### 6. 飞书环境变量未持久化
- **问题**：`FEISHU_BASE_TOKEN` / `FEISHU_TABLE_ID` 未设为 Windows 用户环境变量，pythonw 子进程继承不到
- **修复**：通过 `setx` 持久化为用户环境变量

### 7. 注册表自启动指向错误
- **问题**：注册表 `RestReminder` 自启动指向 `rest_reminder.py`，崩溃后无看门狗重启
- **修复**：改为指向 `watchdog.py`，崩溃自动恢复

### 8. 一键安装.bat 全面修正
- 检测 WindowsApps 代理并自动找真实 Python 路径
- 自动设置飞书环境变量（如未配置）
- 启动看门狗使用检测到的真实 Python

---

**版本: v2.5**
**日期**: 2026-05-26

## 修复

### 1. FeishuSync GBK 编码崩溃（所有飞书操作 100% 失败）
- **问题**：`subprocess.run(capture_output=True, text=True, encoding='utf-8')` 在 Windows pipe 中内部线程用 gbk 解码，遇到非法字节抛 UnicodeDecodeError，所有飞书调用返回码 1
- **修复**：改为二进制捕获后手动 `stdout.decode('utf-8', errors='replace')`，不再依赖 text=True

### 2. import re 位置导致 B站视频获取 100% 失败
- **问题**：`import re` 写在函数兜底线路上，API 正常路径先执行 `re.match()` 报 `NameError`
- **修复**：`import re` 移到文件顶部

### 3. 倒计时浮层冲突
- **问题**：学习倒计时结束后无条件 `hide_overlay()`，关闭了可能正在运行的电脑使用倒计时浮层
- **修复**：加 `if not self._computer_countdown_active` 保护

### 4. 托盘"显示/隐藏"停止 timer 导致后台功能失效
- **问题**：`toggle_visibility()` 和 `hide_to_edge()` 都调用了 `timer.stop()`，使电池监控、电脑使用追踪等后台功能停止更新
- **修复**：隐藏窗口只隐藏，不停止 timer

### 5. 电脑使用保存时机不可靠
- **问题**：`int(hours * 3600) % 60 == 0` 依赖浮点精度+定时器漂移，可能错过保存
- **修复**：改用 tick 计数器（每秒+1，到60保存）

---

**版本: v2.4**
**日期**: 2026-05-26

## 修复

### 1. 飞书同步三大根因一次性修复
- **shell=True（核心修复）**：`lark-cli` 是 npm 安装的 `.cmd` 管道脚本，Python `subprocess.run()` 在 Windows 上必须 `shell=True` 才能执行，否则返回码 1 + 空 stdout，所有飞书操作全废。`_call_lark` 加了 `shell=(sys.platform == 'win32')`
- **分页遍历（防重复创建）**：`_find_today_record()` 改为 `while` 循环 + `--offset N` 逐页遍历到 `has_more: false`，解决表超 200 条后今日记录在第二页被漏掉、误创建新记录的问题
- **二次确认简化**：删除冗余的二次查询逻辑（之前二次确认做的跟一次查询完全一样），分页遍历后直接出结果
- **stderr 日志**：`_call_lark` 增加 stderr 输出记录，方便下次排查 lark-cli 报错
- **.feishu_cache.json 清理**：旧缓存过期日期 2026-05-23 已删除，重启后强制查飞书

---

**版本: v2.3.1**
**日期**: 2026-05-26

## 修复

### 1. 飞书同步找不到今日记录（导致重复创建）
- **问题**：`_find_today_record()` 用 `fields`（中文字段名如'日期'）定位日期列，但 Windows 下 lark-cli JSON 输出的中文字段名编码损坏，导致 `fields.index('日期')` 永远失败，每次都创建重复记录
- **修复**：改用 `field_id_list`（ASCII field ID 如 `fldTXDs0Ro`）定位日期列，不受编码影响
- **同时修复**：lark-cli `+record-list --format json` 返回结构是 `data.data[]` + `data.fields[]` + `data.record_id_list[]`，代码之前用 `resp.get('data', {}).get('data', [])` 正确，但 fields 和 record_id_list 的提取路径有误

---

**版本: v2.3**
**日期**: 2026-05-25

## 修复

### 1. 倒计时浮层不显示
- **问题**：`_play_chime` 被标记为 `@staticmethod`，但内部引用了 `self.show()` / `self.raise_()`，导致 `NameError` 静默崩溃，浮层从未被显示
- **修复**：将 `self.show()` / `self.raise_()` 从 `_play_chime` 移回 `show_countdown`，`_play_chime` 只负责播放音效

### 2. 倒计时浮层无法拖拽
- **问题**：子组件（QLabel、QProgressBar）拦截鼠标事件，导致浮层的 `mousePressEvent` 不触发
- **修复**：为子组件安装 `eventFilter`，在事件过滤器中直接处理拖拽逻辑；新增 `QEvent` 导入

---

**版本: v2.2**
**日期**: 2026-05-24

## 新增功能

### 1. 5分钟倒计时浮层
- 学习倒计时最后5分钟：屏幕顶部显示小型浮窗倒计时
- 电脑使用3小时周期最后5分钟：同上
- 浮层支持拖动、位置记忆、进度条、呼吸动画（最后60秒）、音效提醒
- 非阻塞音效（daemon线程播放）
- 坐标校验（换显示器后自动回退默认位置）

### 2. 电脑使用计数器持久化
- `.computer_usage.json` 缓存文件，跨重启恢复
- 退出时自动保存

## 修复

- FeishuSync 传参修正（传小时数而非周期数）
- 3h 周期结束时正确隐藏倒计时浮层
- 暂停时隐藏浮层，解除对电脑倒计时的阻塞
- 午夜重置清理 countdown flag，防止次日阻塞
- `import math` 改为顶部导入
- 进度条 total 动态计算，重启后首次显示正确

---

**版本: v2.1
**日期**: 2026-05-23

## 修复内容

### 1. 电脑使用计数器跨重启持久化
- **问题**：`computer_usage_hours_today` 在程序重启时归零，飞书同步永远无法触发
- **修复**：新增 `.computer_usage.json` 缓存文件，每 60 秒自动保存，启动时恢复
- **新增字段**：`computer_3h_cycles_today` 追踪已完成的 3 小时周期数

### 2. 飞书同步值修正
- **问题**：传递原始小时数（如 3.0003）而非整数计数
- **修复**：传递已完成的 3 小时周期数（1, 2, 3...），与学习时长逻辑一致

## 修改的文件
- rest_reminder.py（主要修复文件）

---

**版本: v2.0
**日期**: 2026-05-19

## 修复内容

### 2. 修复字段名
- "学习时长" → "学习时长（H）" (全角括号)
- "电脑使用时长 (H)" → "电脑使用时长（H）" (全角括号)

### 3. 修复日期格式
- 字符串格式 → Unix 时间戳 (毫秒)

### 4. 修复重复记录问题
- sync_record(): 增加查找今天记录，如果有则更新，没有则创建

### 5. 添加详细调试日志
- 所有飞书操作添加了详细的调试输出

## 修改的文件
- rest_reminder.py (主要修复文件)

## 新增文件
- verify_feishu.py (验证脚本)
- test_update.py (更新测试脚本)
- test_feishu_sync.py (旧的测试脚本)
- get_tables.py (获取多维表格信息)
- get_wiki_tables.py (获取wiki表格)
- 重启程序.bat
- 飞书权限配置指南.md
- 飞书权限详细指南.md
- 授权指南.md

## 已实现功能
- ✅ 学习1小时自动记录到飞书
- ✅ 电脑使用3小时自动记录到飞书
- ✅ 每天22:00同步当天数据
- ✅ 避免重复记录

## 验证结果
- ✅ 访问权限已配置
- ✅ 访问成功
- ✅ 创建记录成功
